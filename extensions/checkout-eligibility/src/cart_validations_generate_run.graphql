query CartValidationsGenerateRunInput {
  cart {
    cost {
      totalAmount { amount currencyCode }
    }
    buyerIdentity {
      purchasingCompany {
        company { name }
        location { name }
      }
    }
  }
}
